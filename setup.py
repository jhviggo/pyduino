from setuptools import setup, find_packages

setup(
    name="pyduino",
    version="1.0.0",
    description="Python package for controllering arduinos over serial",
    author="Viggo Petersen",
    license="MIT",
    packages=find_packages()
)
